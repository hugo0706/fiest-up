<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Spoty Fest" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <%= render 'layouts/flash_messages' %>

      <div class="bg-black text-gray-200 h-[100vh]">
          <div class="lg:mx-32">
            <%= render 'layouts/header' %>
            <div class="flex flex-col gap-3 mx-1">
                <div class="flex gap-3">
                    <main class="flex-grow w-5/6 sticky top-20 h-[calc(100vh-136px)] p-2 rounded-2xl bg-spotify-gray-dark">
                        <%= yield %>
                    </main>
                </div>
            </div>
            <%= render 'layouts/footer_nav' %>
          </div>
      </div>
  </body>
</html>
