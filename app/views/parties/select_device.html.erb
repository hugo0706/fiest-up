<div data-controller="select-device" data-device-set-url="<%= party_data_set_party_device_path(code: @party.code)%>"  data-device-fetch-url="<%= party_data_party_device_list_path(code: @party.code)%>" class="flex flex-col h-full justify-between items-center">
    <div class="flex flex-col flex-grow items-center justify-center">
        <h1 class="text-4xl font-bold whitespace-normal break-words">Select a device</h1>
        <p class="text-lg text-gray-400  text-center">
            To reproduce the music
        </p>
    </div>
    <span class="flex flex-col justify-start md:justify-center ml-2 text-md text-gray-500 text-left whitespace-normal self-start break-words">
        <span class="text-gray-400 font-semibold">🔎 Don't see your device?</span> 
        <span class="flex items-center gap-2">
          1. Open 
          <%= image_tag 'spotify-full-logo.svg', class: 'w-[80px]' %> 
        </span>
        <span class="">2. Your device should appear here soon</span> 
    </span>
    <div data-select-device-target="devicesGrid" class="h-3/4 md:h-5/6">
        <%= render partial: 'parties/device_list', locals: { devices: @devices } %>
    </div>
    <div class="animate-spin hidden rounded-full h-14 w-14 border-2 border-t-8 border-zinc-400 absolute bottom-20 z-50 place-self-center items-center" data-select-device-target="spinner"></div>
    <p data-select-device-target="setDeviceButton"
       data-action="click->select-device#setDevice"
       class="hidden absolute bottom-20 z-50 px-6 py-4 rounded-lg w-auto place-self-center bg-spotify-green items-center font-bold text-xl hover:bg-green-700 hover:cursor-pointer"
       >
           Use this device
       </p>
</div>
