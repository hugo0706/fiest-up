<%= render 'shared/join_party' %>
<div class="relative flex py-5 items-center w-full">
    <div class="flex-grow border-t border-gray-400"></div>
    <span class="flex-shrink mx-2 text-gray-400 px-4 font-bold text-xl">or</span>
    <div class="flex-grow border-t border-gray-400"></div>
</div>
<% if !!logged_with_spotify? %>
    <%= render 'shared/create_party' %>
<% else %>
    <%= render 'shared/login_with_spotify' %>
<% end %>
