<% songs.each do |song| %>
    <div class="flex items-center justify-between p-1 gap-3 h-14">
        <div class="flex items-center gap-2 flex-grow min-w-0">
            <img src="<%= song[:image] %>" class="w-12 h-12 rounded-sm lg:rounded">
            <div class="overflow-hidden text-ellipsis whitespace-nowrap">
            <p class="font-semibold text-sm truncate"><%= song[:name] %></p>
                <p class="font-semibold text-gray-500 text-xs truncate"><%= song[:artists].join(',')%></p>
            </div>
        </div>
        <div data-action="click->search#addToQueue" data-add-to-queue-url="<%=party_data_add_to_queue_path(code: party_code, spotify_song_id: song[:spotify_song_id])%>"class="hover:bg-spotify-gray-highlight rounded-md p-1">
          <%= image_tag 'add-to-queue-icon.svg', class: 'w-8 h-8 mr-2' %>
        </div>
    </div>
    <hr class="border-t border-spotify-gray-highlight opacity-30 my-1">
<% end %>
